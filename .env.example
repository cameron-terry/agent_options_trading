# Copy to .env and fill in before running `docker compose up -d options-agent`.
# .env is gitignored — never commit real credentials.

ALPACA_API_KEY=
ALPACA_SECRET_KEY=
ANTHROPIC_API_KEY=
DISCORD_WEBHOOK_URL=